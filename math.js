//calculate the area of ​​the circle knowing that the formula is: A = π r²
const raio = 5.6
const area = Math.PI * Math.pow(raio, 2);
console.log("a área total é ", area);
/* Math.pow é usado para o caso da exponenciação. 
Aqui siginifica raio elevado ao quadrado.
Math.pow is used for exponentiation.
Here, it means radius squared.*/